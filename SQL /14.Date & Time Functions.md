Here is the **Date & Time Functions** section — explained clearly and simply.

---

# 🟦 **Date & Time Functions**

Date & time functions help you:

👉 Get the current date/time
👉 Extract parts like day/month/year
👉 Add or subtract days/months
👉 Calculate differences between dates
👉 Format dates in readable form

Let’s go through each one in simple, understandable language.

---

# 🟩 **1) NOW() / CURRENT_TIMESTAMP**

**Meaning:**
“Give me the current date and time right now.”

**Explanation:**

* Returns date + time (YYYY-MM-DD HH:MM:SS)
* Useful for timestamps, logs, created_at, updated_at

**Example:**

```sql
SELECT NOW();
```

This means:
➡️ Show the exact current date and time.

---

# 🟩 **2) CURDATE() / CURRENT_DATE**

**Meaning:**
“Give me today’s date only.”

**Example:**

```sql
SELECT CURDATE();
```

This means:
➡️ Show today's date without time.

---

# 🟩 **3) CURTIME()**

**Meaning:**
“Give me the current time only.”

**Example:**

```sql
SELECT CURTIME();
```

This means:
➡️ Show the current time without the date.

---

# 🟩 **4) DATE()**

**Meaning:**
“Extract only the date part from a datetime.”

**Example:**

```sql
SELECT DATE('2024-02-10 14:22:10');
```

This means:
➡️ Remove the time, keep only the date → `2024-02-10`.

---

# 🟩 **5) TIME()**

**Meaning:**
“Extract only the time from a datetime.”

**Example:**

```sql
SELECT TIME('2024-02-10 14:22:10');
```

This means:
➡️ Return only the time → `14:22:10`.

---

# 🟩 **6) EXTRACT()**

**Meaning:**
“Pick one specific part from a date.”

**Examples:**

```sql
SELECT EXTRACT(YEAR FROM order_date);
SELECT EXTRACT(MONTH FROM order_date);
SELECT EXTRACT(DAY FROM order_date);
```

This means:
➡️ Get the year / month / day from the `order_date` column.

---

# 🟩 **7) DATE_ADD()**

**Meaning:**
“Add a certain amount of time to a date.”

**Explanation:**
You can add days, months, years, etc.

**Example:**

```sql
SELECT DATE_ADD('2024-01-01', INTERVAL 10 DAY);
```

This means:
➡️ Add 10 days → `2024-01-11`.

---

# 🟩 **8) DATE_SUB()**

**Meaning:**
“Subtract a certain amount of time from a date.”

**Example:**

```sql
SELECT DATE_SUB('2024-01-15', INTERVAL 5 DAY);
```

This means:
➡️ Subtract 5 days → `2024-01-10`.

---

# 🟩 **9) DATEDIFF()**

**Meaning:**
“Tell me how many days are between two dates.”

**Example:**

```sql
SELECT DATEDIFF('2024-02-10', '2024-02-01');
```

This means:
➡️ 9 days difference.

---

# 🟩 **10) TIMESTAMPDIFF()**

**Meaning:**
“Difference but with custom units (days, months, years).”

**Example:**

```sql
SELECT TIMESTAMPDIFF(MONTH, '2023-01-01', '2024-06-01');
```

This means:
➡️ Number of months between the two dates.

---

# 🟩 **11) DATE_FORMAT()**

**Meaning:**
“Show the date in a nice readable format.”

**Example:**

```sql
SELECT DATE_FORMAT(NOW(), '%d-%m-%Y');
```

This means:
➡️ Format date as `DD-MM-YYYY`.

Common format codes:

* `%d` → Day
* `%m` → Month
* `%Y` → 4-digit year
* `%H` → Hour
* `%i` → Minutes

---

# 🟩 **12) STR_TO_DATE()**

**Meaning:**
“Convert text into a proper date.”

**Example:**

```sql
SELECT STR_TO_DATE('10-02-2024', '%d-%m-%Y');
```

This means:
➡️ Convert string into an actual date object.

---

# 🟦 **Summary Table**

| Function        | What it Does                   |
| --------------- | ------------------------------ |
| NOW()           | Current date + time            |
| CURDATE()       | Only today's date              |
| CURTIME()       | Only current time              |
| DATE()          | Extract date                   |
| TIME()          | Extract time                   |
| EXTRACT()       | Extract parts like year, month |
| DATE_ADD()      | Add days/months/years          |
| DATE_SUB()      | Subtract time                  |
| DATEDIFF()      | Difference in days             |
| TIMESTAMPDIFF() | Difference in any unit         |
| DATE_FORMAT()   | Format date                    |
| STR_TO_DATE()   | Convert string → date          |

---

# ✔️ Next Section?

Choose the next one:

👉 **Conditional Logic (CASE, COALESCE, NULLIF)**
Just reply: **Conditional Logic**
