Here is the **Conditional Logic** section — explained clearly, simply, and in the same style.

---

# 🟦 **Conditional Logic (CASE, COALESCE, NULLIF)**

Conditional logic lets SQL behave like **if-else statements** inside queries.

You use these when you want SQL to:

👉 Check conditions
👉 Return different values based on those conditions
👉 Handle NULL values
👉 Prevent errors

Let’s break each one down.

---

# 🟩 **1) CASE (SQL's IF-ELSE)**

**Meaning:**
“Check conditions in order and return a value based on the first true condition.”

**Explanation:**

* Works like **if → else if → else**
* Can be written inside SELECT, WHERE, ORDER BY
* Very powerful for formatting or categorizing data

**Example:**

```sql
SELECT name, age,
       CASE
           WHEN age < 18 THEN 'Minor'
           WHEN age < 60 THEN 'Adult'
           ELSE 'Senior'
       END AS age_group
FROM users;
```

This means:
➡️ Categorize each user based on age into Minor, Adult, or Senior.

---

## **Simple CASE Example**

```sql
SELECT product,
       CASE category
           CASE 'E' THEN 'Electronics'
           CASE 'F' THEN 'Fashion'
           ELSE 'Other'
       END AS category_name
FROM products;
```

➡️ Convert category codes into readable labels.

---

# 🟩 **2) COALESCE(value1, value2, ...)**

**Meaning:**
“Return the first value that is *not NULL*.”

**Explanation:**

* Useful when data might be missing
* Checks values from left to right
* Returns the first non-NULL value

**Example:**

```sql
SELECT name,
       COALESCE(phone, 'Not Provided') AS phone_number
FROM users;
```

This means:
➡️ If phone is NULL, show “Not Provided”.

Another example:

```sql
COALESCE(email_secondary, email_primary)
```

➡️ Prefer secondary email if available, otherwise use primary.

---

# 🟩 **3) NULLIF(value1, value2)**

**Meaning:**
“Return NULL if both values are equal; return value1 otherwise.”

**Explanation:**

* Used to **avoid division by zero errors**
* Useful when checking duplicates
* Returns NULL when two values match exactly

**Example 1: Avoid division by zero**

```sql
SELECT total / NULLIF(count, 0)
FROM sales;
```

This means:
➡️ If count = 0 → return NULL instead of throwing an error.

---

**Example 2: Detect duplicates**

```sql
SELECT NULLIF(username, 'admin') 
FROM users;
```

This means:
➡️ If username = 'admin', return NULL.
➡️ Otherwise return the username.

---

# 🟦 Quick Summary Table

| Function     | What it Means        | What it Returns                |
| ------------ | -------------------- | ------------------------------ |
| **CASE**     | If-else logic        | Value based on condition       |
| **COALESCE** | First non-NULL       | Cleans up missing data         |
| **NULLIF**   | Return NULL if equal | Prevents divide-by-zero errors |

---

# ✔️ All Major SQL Topics Completed!

You've covered:

* Basic SQL
* Filtering
* Sorting
* Aggregations
* Joins
* Subqueries
* Set Operations
* Indexing
* Transactions
* Views
* Triggers
* CTEs
* Window Functions
* Date Functions
* Conditional Logic ✔️

---

If you want, I can now:

👉 **Combine all sections into ONE professional GitHub-ready README**
👉 **Add Navigation + Table of Contents**
👉 **Add styling (headers, emojis, icons, code blocks, separators)**

Just reply:

**“Make final combined README”**
